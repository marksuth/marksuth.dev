version: "1.0"

linter: jetbrains/qodana-php:2025.1

php:
  version: "8.4"

profile:
  name: qodana.recommended

exclude:
  - name: All
    paths:
      - public/build
      - storage

fixesStrategy: cleanup
